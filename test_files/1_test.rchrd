const {
    string name = "Alexandre", placa = "yyy-0000";
    int b = 32;
}

class Pokemon{
    cuscuz.iniciarPreparo(1, receita, cuscuz.getModoPreparo());

    variables {
        int life[2] = {(12, 12)}, iv[4] = {(10, 21, 7, 19)};
        int idade = 2018-1997;
        int peso = 75;
        int imc  = peso/(1.78*1.78);
        string name;
        float pronto = cuscuz.statusCuscuz();        
    }

    method bool attack(int enemyLife){
        cuscuz.getPower(cuscuz.getAttack());
    
        variables {
            int newLife;
        }
        newLife = enemyLife - iv[2];
        return newLife > 0;
    }

    method void sleep(){
	life[1] = life[2];
        cuscuz = milho.getEssencia();
    }

}
