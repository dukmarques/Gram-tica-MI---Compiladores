    class Pokemon{
    
    cuscuz.iniciarPreparo(1, receita, cuscuz.getModoPreparo());

    variables {
        int life[2] = {(12, 12)}, iv[4] = {(10, 21, 7, 19)};
        int idade = 2018-1997;
        int peso = 75;
        int imc  = peso/(1.78*1.78);
        string name;
        float pronto = cuscuz.statusCuscuz();        
    }
    
    method void tasmanianDevil(int giros){
        if(giros > 50 && velocidade == 10)then{
            nota.dez = 10;
            cuscuz.nota = nota.dez;
            read(Taz_Loucao, d.f, h.e); 
            write(Taz_Loucao, d.f);
                   
        }else if(giros == 0 || !(velocidade))then{
            write(Taz_Dormiu);        
        }
    }

    method bool attack(int enemyLife){
        cuscuz.getPower(cuscuz.getAttack());
    
        variables {
            int newLife;
        }
        newLife = enemyLife - iv[2];
        return newLife > 0;
    }

    method void sleep(){
        life[1] = life[2];
        cuscuz = milho.getEssencia();
    }

}
